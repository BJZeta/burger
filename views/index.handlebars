<h1>Burgers</h1>

<h2>Burgers that are just sitting there, not being eaten</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{ burger_name }}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been devoured</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{ burger_name }}
    {{/if}}
  {{/each}}
</ul>

<h2>Add Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="newB">Burger:</label>
    <input type="text" id="newB" name="burgers_name">
  </div>

  <button type="submit">Add Burger</button>
</form>
